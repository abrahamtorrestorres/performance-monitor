apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  labels:
    app: intel-cloud-performance-service
data:
  db-host: "postgres-service"
  db-port: "5432"
  db-name: "performance_db"
  redis-url: "redis://redis-service:6379"
  log-level: "info"

